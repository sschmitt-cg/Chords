Take a look at the documentation to familiarize yourself with the project. Then, take a look at any currently open GitHub issues. Think about where we are in terms of the implementation plan and the GitHub issues, and think about what the next logical step we should take.

Once you have a good idea of what the correct next step should be, I would like to create a new branch, and do the necessary work.

When you have completed that, create a pull request targeted to merge into the `main` branch. Make sure the PR description accurately reflects the work. If you are addressing a GitHub issue, make sure to link the GitHub issue to the PR, so when the PR merges, the issue closes.

If it hasn't already been requested automatically, request a review from GitHub Copilot.

Then click Subscribe in the PR sidebar (or otherwise enable notifications for that PR) so you can receive any feedback and watch the CI/CD processes to make sure all tests pass. If you get feedback on the PR, respond to the feedback as appropriate. If you see failing tests, correct the tests and push the fixes to the branch. Continue watching for feedback and testing results until I merge the PR. Any time you push changes to the branch, re-request review from GitHub Copilot, but limit yourself to five rounds of review. If after five rounds, there are still major outstanding issues, ask me what to do.