// Generic picker modal (used for key, mode, scale type selection)
// TODO: migrate from #wheelModal pattern in index-legacy.html + setupPickerModal() in script.js

export default function WheelModal() {
  return <div />
}
