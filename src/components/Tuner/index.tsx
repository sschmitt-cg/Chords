// Chromatic tuner — microphone pitch detection with guided tuning-to-preset mode
// TODO: migrate from tunerState + tunerLoop() + updateTunerUI() in script.js
//       add guided mode: reads active guitarTuning from store to show per-string targets

export default function Tuner() {
  return <div />
}
