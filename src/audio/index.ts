// Web Audio engine: oscillator synth, metronome scheduler, tuner autocorrelation
// Migrating from script.js. Web Audio API works natively in Capacitor WKWebView.
//
// TODO (in order):
//   1. ensureAudio(), unlockAudioGestureSync(), warmupAudioOnce()
//   2. playSynthNote(), playSequence(), playStrum()
//   3. scheduleMetronomeTick(), metronomeScheduler(), startMetronome(), stopMetronome()
//   4. autocorrelate(), tunerLoop(), startTuner(), stopTuner()
//   5. playReferenceTone()

export {}
